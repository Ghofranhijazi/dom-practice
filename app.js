// selecting where to add the element
let list = document.querySelector("ul");
// creating an element to add 
let item = document.createElement("li");
// adding element to the DOM
list.appendChild(item);
// writing contant in the element (optional)
//  item.textContent="hareth";